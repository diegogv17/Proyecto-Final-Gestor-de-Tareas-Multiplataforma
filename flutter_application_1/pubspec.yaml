name: flutter_application_1
description: "STACKFLOW - Gestor de tareas conectado al backend API"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.1
  go_router: ^17.2.3
  google_fonts: ^8.1.0
  flutter_animate: ^4.5.2
  intl: ^0.20.2
  uuid: ^4.5.3
  shimmer: ^3.0.0
  dio: ^5.4.0
  flutter_secure_storage: ^10.3.0
  table_calendar: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
